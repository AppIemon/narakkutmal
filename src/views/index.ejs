<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>나라 이름 끝말잇기</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <header>
            <h1>🌍 나라 이름 끝말잇기</h1>
            <p class="subtitle">UN 승인국으로 끝말잇기를 즐겨보세요!</p>
        </header>

        <!-- 로그인/회원가입 화면 -->
        <div id="loginScreen" class="screen active">
            <div class="auth-container">
                <!-- 로그인 폼 -->
                <div id="loginForm" class="auth-form active">
                    <h2>로그인</h2>
                    <input type="text" id="loginUsername" placeholder="사용자 이름" maxlength="20" autocomplete="username">
                    <input type="password" id="loginPassword" placeholder="비밀번호" autocomplete="current-password">
                    <button id="loginBtn" class="btn btn-primary">로그인</button>
                    <p class="auth-switch">
                        계정이 없으신가요? <a href="#" id="showRegister">회원가입</a>
                    </p>
                </div>
                
                <!-- 회원가입 폼 -->
                <div id="registerForm" class="auth-form">
                    <h2>회원가입</h2>
                    <input type="text" id="registerUsername" placeholder="사용자 이름 (3-20자)" maxlength="20" autocomplete="username">
                    <input type="password" id="registerPassword" placeholder="비밀번호 (6자 이상)" autocomplete="new-password">
                    <input type="password" id="registerPasswordConfirm" placeholder="비밀번호 확인" autocomplete="new-password">
                    <button id="registerBtn" class="btn btn-primary">회원가입</button>
                    <p class="auth-switch">
                        이미 계정이 있으신가요? <a href="#" id="showLogin">로그인</a>
                    </p>
                </div>
            </div>
        </div>

        <!-- 메인 메뉴 -->
        <div id="menuScreen" class="screen">
            <div class="user-info">
                <h3>안녕하세요, <span id="displayUsername"></span>님!</h3>
                <div class="tier-badge" id="tierBadge" style="display: none;">
                    <span id="tierName"></span>
                </div>
                <div class="stats">
                    <div class="stat-item">
                        <span class="stat-label">레이팅</span>
                        <span class="stat-value" id="userRating">1500</span>
                    </div>
                    <div class="stat-item">
                        <span class="stat-label">승</span>
                        <span class="stat-value" id="userWins">0</span>
                    </div>
                    <div class="stat-item">
                        <span class="stat-label">패</span>
                        <span class="stat-value" id="userLosses">0</span>
                    </div>
                    <div class="stat-item">
                        <span class="stat-label">무</span>
                        <span class="stat-value" id="userDraws">0</span>
                    </div>
                </div>
            </div>
            <div class="menu-buttons">
                <button id="singlePlayerBtn" class="btn btn-large btn-primary">싱글플레이</button>
                <button id="multiPlayerBtn" class="btn btn-large btn-secondary">멀티플레이</button>
                <button id="rankingBtn" class="btn btn-large btn-secondary">랭킹</button>
            </div>
            <div id="matchingStatus" class="matching-status hidden">
                <p>매칭 중...</p>
                <button id="cancelMatchBtn" class="btn btn-small">취소</button>
            </div>
            <div class="logout-section">
                <button id="logoutBtn" class="btn btn-small">로그아웃</button>
            </div>
        </div>

        <!-- 게임 화면 -->
        <div id="gameScreen" class="screen">
            <div class="game-header">
                <div class="game-mode" id="gameMode">싱글플레이</div>
                <div class="game-turn" id="gameTurn">당신의 턴</div>
            </div>
            
            <div class="game-board">
                <div class="used-countries" id="usedCountries">
                    <h3>사용된 국가</h3>
                    <div class="country-list" id="countryList"></div>
                </div>
                
                <div class="current-country">
                    <div class="label">마지막 국가</div>
                    <div class="country-name" id="lastCountry">-</div>
                </div>
                
                <div class="input-section">
                    <input type="text" id="countryInput" placeholder="나라 이름을 입력하세요" autocomplete="off" disabled>
                    <button id="submitBtn" class="btn btn-primary">제출</button>
                </div>
                
                <div class="timer" id="timer">30초</div>
                
                <div class="surrender-section">
                    <button id="surrenderBtn" class="btn btn-small btn-danger">기권</button>
                </div>
            </div>
            
            <button id="backToMenuBtn" class="btn btn-small">메뉴로 돌아가기</button>
        </div>

        <!-- 랭킹 화면 -->
        <div id="rankingScreen" class="screen">
            <div class="ranking-container">
                <h2>🏆 랭킹</h2>
                <div class="user-rank-info" id="userRankInfo">
                    <div class="rank-item user-rank">
                        <span class="rank-number" id="userRank">-</span>
                        <span class="rank-username" id="userRankUsername">-</span>
                        <span class="rank-rating" id="userRankRating">-</span>
                        <span class="rank-tier" id="userRankTier">-</span>
                    </div>
                </div>
                <div class="ranking-list" id="rankingList">
                    <div class="loading">랭킹을 불러오는 중...</div>
                </div>
                <button id="backToMenuFromRankingBtn" class="btn btn-primary">메뉴로 돌아가기</button>
            </div>
        </div>

        <!-- 게임 종료 화면 -->
        <div id="gameEndScreen" class="screen">
            <div class="game-end-box">
                <h2 id="gameEndTitle">게임 종료</h2>
                <p id="gameEndMessage"></p>
                <div id="ratingChanges" class="rating-changes hidden">
                    <h3>레이팅 변화</h3>
                    <div id="ratingChangeDetails"></div>
                </div>
                <button id="backToMenuAfterEndBtn" class="btn btn-primary">메뉴로 돌아가기</button>
            </div>
        </div>

        <!-- 알림 메시지 -->
        <div id="notification" class="notification hidden"></div>
    </div>

    <script src="/socket.io/socket.io.js"></script>
    <script src="/js/app.js"></script>
</body>
</html>

